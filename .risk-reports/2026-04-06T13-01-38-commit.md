

## Pipeline Risk Report

### Layer 1: Unreleased Changes (release risk)
- Scope: No unreleased changesets pending (the changeset was deleted, so no version bump will be triggered)
- Risks: Minimal — this is a CI fix with no published package changes

**Residual risk: 1/25 (Very Low)**

### Layer 2: Unreleased + Unpushed (push risk)
- Scope: Pin TypeScript to ~5.6.3, revert two type annotations to plain `Uint8Array`, remove redundant tsconfig lib entry, delete changeset, update lockfile

#### Risk 1: TypeScript version pin restricts future upgrades
- Inherent impact: 2/5 (Minor) - pinning to ~5.6.x means no auto-upgrade to 5.7+, could delay adoption of new TS features
- Inherent likelihood: 2/5 (Unlikely) - this is intentional and well-understood; upgrading later is straightforward
- Inherent risk: 4/25 (Low)
- Controls:
  - All 236 tests pass with the pinned version — confirms no regression from the pin
  - CI lint (xo) passes with 0 errors — confirms the root cause (xo parser incompatibility with TS 5.7+) is resolved
- **Residual risk: 2/25 (Very Low)**

#### Risk 2: Type annotation revert loses type precision
- Inherent impact: 1/5 (Negligible) - `Uint8Array<ArrayBuffer>` generic parameter was only available in TS 5.7+; reverting to `Uint8Array` is the standard type and correct for TS 5.6
- Inherent likelihood: 1/5 (Rare) - the generic form was not needed and all tests pass
- Inherent risk: 1/25 (Very Low)
- Controls:
  - Vitest test suite (236 tests) exercises fetch-fragment and ofetch-link response handling
  - TypeScript strict mode compilation succeeds for all 7 packages
- **Residual risk: 1/25 (Very Low)**

**Cumulative residual risk: 2/25 (Very Low)**

### Layer 3: Unreleased + Unpushed + Uncommitted (commit risk)
- Scope: Same as Layer 2 — all changes are uncommitted per the git status (modified package.json plus untracked files)
- Additional risks from uncommitted changes: None beyond what is already assessed. The untracked files (.claude/, AGENTS.md, BRIEFING.md, CLAUDE.md, DECISION-MANAGEMENT.md, PRINCIPLES.md, RISK-POLICY.md, scripts/) are project configuration/documentation, not part of this commit action.

**Cumulative residual risk: 2/25 (Very Low)**

### Pipeline Summary
| Layer | Scope | Residual Risk |
|-------|-------|--------------|
| Unreleased | No changesets — no publish impact | 1/25 (Very Low) |
| + Unpushed | TS pin + type reverts + tsconfig cleanup | 2/25 (Very Low) |
| + Uncommitted | Same (all changes in this commit) | 2/25 (Very Low) |

### Assessment

This is a **risk-reducing** change. It fixes a pre-existing CI lint failure caused by TypeScript version drift breaking xo's parser. The changes are conservative (version pin, type revert, config cleanup), well-tested (236 tests, 7 package builds, 0 lint errors), and do not trigger any published version changes.

RISK_SCORES: commit=2 push=2 release=1
RISK_BYPASS: reducing
