This is a minimal, single-string fix to a CI configuration file. Let me check what's uncommitted and unpushed.

## Pipeline Risk Report

### Layer 1: Unreleased Changes (release risk)
- Scope: Recent dependency updates (cspell, turbo 2.x, @types/node, TypeScript pinning, TS 5.9 compat) plus this CI reporter fix
- Risks: Dependency bumps carry minor integration risk, but these are dev dependencies only
- **Residual risk: 2/25 (Very Low)**

### Layer 2: Unreleased + Unpushed (push risk)
- Scope: Same as Layer 1 (no additional unpushed changes beyond this commit)
- Additional risks from unpushed changes: None beyond the commit being scored
- **Cumulative residual risk: 2/25 (Very Low)**

### Layer 3: Unreleased + Unpushed + Uncommitted (commit risk)
- Scope: Single string change in `vite.config.ci.ts` — renaming reporter from `'basic'` to `'default'`

#### Risk 1: CI reporter misconfiguration
- Inherent impact: 2/5 (Minor) - Affects CI reporting only, not runtime behavior or published packages
- Inherent likelihood: 1/5 (Rare) - Single string literal change matching vitest 4.x documented API
- Inherent risk: 2/25 (Very Low)
- Controls:
  - Developer confirmed all tests pass locally with coverage - reduces likelihood from 1 to 1 (already rare)
  - CI pipeline itself will validate the reporter name on next run - catches failure before release
- **Residual risk: 2/25 (Very Low)**

- **Cumulative residual risk: 2/25 (Very Low)**

### Pipeline Summary
| Layer | Scope | Residual Risk |
|-------|-------|--------------|
| Unreleased | Dev dependency updates + CI fix | 2/25 (Very Low) |
| + Unpushed | Same | 2/25 (Very Low) |
| + Uncommitted | Reporter string change in vite.config.ci.ts | 2/25 (Very Low) |

This is a risk-reducing change: it fixes a CI failure caused by an API rename in vitest 4.x. No runtime code, no published package output, no user-facing impact.

RISK_SCORES: commit=2 push=2 release=2
RISK_BYPASS: reducing
