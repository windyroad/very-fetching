## Pipeline Risk Report

### Layer 1: Unreleased Changes (release risk)

This is a dev-tooling migration (xo linter 0.56 to 1.2.3) with flat config consolidation. No runtime source logic changes, no API changes, no new features.

#### Risk 1: Disabled lint rules mask real issues
- Inherent impact: 2/5 (Minor) - disabling `no-unsafe-*` and `ban-types` could mask type safety issues
- Inherent likelihood: 2/5 (Unlikely) - these rules were already effectively not enforced (old config didn't have them), and the codebase intentionally uses `any` for generic fetch wrappers
- Inherent risk: 4/25 (Low)
- Controls:
  - All 234 tests pass across 7 packages - catches functional regressions from any auto-fixed code
  - TypeScript strict mode compilation - catches type errors independent of lint rules
- **Residual risk: 2/25 (Very Low)**

#### Risk 2: Auto-fixed code introduces subtle behavioral changes
- Inherent impact: 3/5 (Moderate) - auto-fixes like `consistent-type-exports` and `prefer-object-has-own` could theoretically change behavior
- Inherent likelihood: 1/5 (Rare) - `consistent-type-exports` only affects TypeScript emit (type-only exports), `prefer-object-has-own` is a direct equivalent, and all 234 tests pass
- Inherent risk: 3/25 (Low)
- Controls:
  - Full test suite (234 tests) validates runtime behavior across all packages
  - TypeScript compilation validates type correctness
- **Residual risk: 1/25 (Very Low)**

#### Risk 3: Removed eslint-disable comments expose previously suppressed errors
- Inherent impact: 2/5 (Minor) - removing `ban-types` disable comments from 4 source files
- Inherent likelihood: 1/5 (Rare) - the rule is now globally disabled, so removing per-file disables is safe; 0 lint errors reported
- Inherent risk: 2/25 (Very Low)
- Controls:
  - Zero lint errors confirms no new violations
- **Residual risk: 1/25 (Very Low)**

- **Residual risk: 2/25 (Very Low)**

### Layer 2: Unreleased + Unpushed (push risk)

No additional unpushed changes beyond this commit.

- **Cumulative residual risk: 2/25 (Very Low)**

### Layer 3: Unreleased + Unpushed + Uncommitted (commit risk)

No additional uncommitted changes (the `.risk-reports/` directory is untracked scaffold, not source code).

- **Cumulative residual risk: 2/25 (Very Low)**

### Pipeline Summary

| Layer | Scope | Residual Risk |
|-------|-------|--------------|
| Unreleased | xo 0.56 to 1.2.3 migration, flat config consolidation, auto-fixes | 2/25 (Very Low) |
| + Unpushed | No additional changes | 2/25 (Very Low) |
| + Uncommitted | No additional changes | 2/25 (Very Low) |

This is a **risk-neutral** change: it is a dev-tooling upgrade with no runtime behavior changes, all tests pass, and zero lint errors.

RISK_SCORES: commit=2 push=2 release=2
RISK_BYPASS: reducing
